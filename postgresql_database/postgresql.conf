# Repository reference file.
# The running container uses /var/lib/postgresql/data/postgresql.conf (generated/managed by startup.sh).
#
# Minimal intended settings for Kavia runtime:

listen_addresses = '*'
port = 3002

# Container-friendly logging (may be overridden by runtime defaults)
logging_collector = off
